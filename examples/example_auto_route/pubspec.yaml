name: example_auto_route
description: A complex example demonstrating AutoRoute integration with Modularity.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=3.9.0 <4.0.0'

# Temporarily excluded from workspace due to auto_route_generator ^9.0.0
# requiring analyzer <8.0.0, which conflicts with flutter_test (analyzer >=8.0.0).
# Will be re-included when auto_route_generator updates to support analyzer >=8.0.0.
# resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^9.0.0
  modularity_flutter:
    path: ../../packages/flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.8
  auto_route_generator: ^9.0.0







